The second project of Machine Learning Nanodegree 

Python 2.7

System librarries:
     Numpy
     pandas
     time (time)
     IPython.display (display) # Allows the use of display() for DataFrames
     matplotlib 
     
Exterior librarries:
     visuals.py 
     
